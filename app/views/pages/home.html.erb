<div class="home-banner">
  <div class="home-infos">
    <h1>CAPSULE</h1>
      <% if current_user  %>
        <div class= "btn-capsule">
          <%= link_to "Commencer", collections_path, class: "btn-devise" %>
        </div>
        <%= link_to "Déconnexion", destroy_user_session_path, class: "btn-delete", html5: true, data: { turbo_method: :delete, turbo_confirm: "Voulez-vous vous déconnecter ?" } %>
      <% else %>
        <div class= "btn-capsule">
<%= link_to "Connexion avec Google", user_google_oauth2_omniauth_authorize_path, class: "btn-devise", data: { turbo: false } %>
        </div>
      <% end %>
  </div>
</div>
